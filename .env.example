# ContentHive — Environment Variables
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to source control.

# ─── Supabase ─────────────────────────────────────────────────────────────────
# Found in: Supabase Dashboard → Project Settings → API

# Public: safe to expose to the browser
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<your-anon-key>

# Private: server-only — NEVER prefix with NEXT_PUBLIC_
# Used only in API routes and server actions
SUPABASE_SERVICE_ROLE_KEY=<your-service-role-key>

# ─── App ──────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ─── Security ─────────────────────────────────────────────────────────────────
# Restrict registration to users who know this code.
# Leave empty (or remove) to allow open registration.
# Rotate weekly: update this value in Vercel env vars dashboard.
# Existing accounts are NOT affected by rotation — only new registrations need the current code.
INVITE_CODE=

# ─── Encryption ───────────────────────────────────────────────────────────────
# Required for BYOK LLM chat — encrypts user API keys at rest (AES-256-GCM).
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=

# ─── Notifications ────────────────────────────────────────────────────────────
# Create a bot via Telegram @BotFather, then paste the token here.
# Users enter their personal chat ID on their profile page to opt in.
TELEGRAM_BOT_TOKEN=
